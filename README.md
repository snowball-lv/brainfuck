Assembly practice using brainfuck.

## Usage

```bash
./bin/brainfuck [options] file
```

### Options

- `-c` default, compiles to NASM and prints to stdout
- `-i` interpret using ASM interpreter
- `-ic` interpret using C interpreter

## Build

```bash
make
```
